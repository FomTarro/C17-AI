class Move {
	/* possible callback functions not included:
	 * basePowerCallback
	 * onHit
	 * onTryHit
	*/

	constructor (num, accuracy, basePower, category, desc, shortDesc, id, 
		name, pp, priority, flags, isZ, critRatio, secondary, target, type,
		contestType, drain, zMovePower, isViable, zMoveEffect, volatileStatus,
		effect, multiHit, boosts, zMoveBoost, stallingMove)
	{

	}
}